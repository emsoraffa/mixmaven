# CORE module

This module contains the central classes used in our application. The module contains 3 classes. The Drink and ingredient classes are data oriented classes and the Constants class is used for storing global variables.
